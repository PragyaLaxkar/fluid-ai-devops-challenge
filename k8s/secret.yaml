# NOTE: For the demo only. In production this would come from
# Sealed Secrets, External Secrets Operator, or Vault — NEVER committed.
apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: devops-challenge
type: Opaque
stringData:
  POSTGRES_USER: appuser
  POSTGRES_PASSWORD: changeme-in-prod
  POSTGRES_DB: appdb
